# Backend API URL
# Development: use your machine's local IP (run `ipconfig` on Windows / `ifconfig` on Mac/Linux)
# Production: use your deployed backend URL (e.g. https://api.yourdomain.com)
EXPO_PUBLIC_API_URL=http://192.168.x.x:3333
